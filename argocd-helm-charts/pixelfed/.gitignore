# Lock files generated by Helm
Chart.lock
# Note: charts/ directory is NOT ignored because upstream chart has a bug that we patched
# Bug: configmap_env.yaml uses .Values inside a `with` block incorrectly
# See line 50-51 of charts/pixelfed/templates/configmap_env.yaml
