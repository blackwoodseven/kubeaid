{{- if .Values.gfetch.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "puppetserver.name" . }}-gfetch
data:
  config.yaml: |-
    {{- dict "defaults" .Values.gfetch.defaults "repos" .Values.gfetch.repos | toYaml | nindent 4 }}
{{- end }}
