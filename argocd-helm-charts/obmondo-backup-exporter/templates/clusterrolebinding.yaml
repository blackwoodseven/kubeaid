apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "obmondo-backup-exporter.fullname" . }}
  labels:
    {{- include "obmondo-backup-exporter.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "obmondo-backup-exporter.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "obmondo-backup-exporter.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}   # ← Critical: must match deployment namespace
