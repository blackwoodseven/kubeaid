apiVersion: v1
kind: Service
metadata:
  name: {{ include "ox-common.names.fullname" . }}
  labels:
    {{- include "ox-common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
      protocol: TCP
      name: http
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: httplive
    {{- if .Values.remoteDebug.enabled }}
    - port: {{ .Values.remoteDebug.port | default 8101 }}
      targetPort: {{ .Values.remoteDebug.port | default 8101 }}
      protocol: TCP
      name: debug
    {{- end }}
  selector:
    {{- include "ox-common.labels.matchLabels" . | nindent 4 }}
