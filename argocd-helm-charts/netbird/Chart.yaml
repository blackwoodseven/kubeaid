apiVersion: v2
name: netbird
version: 1.1.0
dependencies:
  - name: netbird
    version: 1.9.0
    repository: https://netbirdio.github.io/helms
  # Obmondo's fork of open-engineering/coturn-chart. It carries the DaemonSet
  # template fix, the pod scheduling fields and the DATABASE_* env gate that
  # upstream 10.1.0 lacks: without them the DaemonSet does not render at all and
  # a default install never starts.
  - name: coturn
    version: 10.2.0
    repository: oci://ghcr.io/obmondo/charts
  - name: kubeaid-addons
    version: 0.1.0
    repository: file://../kubeaid-addons
